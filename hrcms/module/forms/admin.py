# -#- coding: utf-8 -#-

from django.contrib import admin
from django.conf import settings

from django.utils.translation import get_language, ugettext_lazy as _

"""
if 'form_designer' in settings.INSTALLED_APPS:
    from form_designer.models import Form, FormSubmission
    from form_designer.admin import FormAdmin, FormSubmissionAdmin

    webcms_admin.register(Form, FormAdmin)
    webcms_admin.register(FormSubmission, FormSubmissionAdmin)
"""
